# clippy configuration for the Arxia workspace.
#
# LOW-014 (commit 084): reviewed and pinned. The MSRV below
# matches `rust-toolchain.toml` and the workspace Cargo.toml ;
# bumping it requires updating both files in lock-step.

# Minimum supported Rust version. Must move in lock-step with
# rust-toolchain.toml, the workspace Cargo.toml, targets/esp32,
# and the LOW-014 pin test in arxia-core. Before bumping, check
# the Xtensa toolchain (esp-rs/rust-build) has a matching
# release — it tracks stable with a lag, and the T-Beam target
# depends on it.
msrv = "1.89.0"
